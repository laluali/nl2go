import { AgeInYearDirective } from './age-in-year.directive';

describe('AgeInYearDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeInYearDirective();
    expect(directive).toBeTruthy();
  });
});
